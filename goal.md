# goal.md — Neus

KPI / マイルストーン。現行 **v0.13.0(出荷済み)**。
現行仕様の正は `SPEC.md`、変更履歴は `CHANGELOG.md`、設計判断は `docs/adr/`。

## North Star

自分の情報を一箇所に集約・正規化・検索・既存知識と接続する個人情報ハブ。
サーバーレス・端末内完結・個人データのサーバー送信ゼロ。

新機能は次の4問(North Star チェック)を通過すること:

1. 端末内完結の不変条件(個人データのサーバー送信ゼロ)を守るか
2. ゼロ依存原則(外部ライブラリ追加は要 ADR)に反しないか
3. 3案比較で選定したか(慣性に抵抗する)
4. 視線フロー「見 → 比較 → 行動」を3ステップ以内に保つか

## 不変条件(G0、常時 PASS)

- 個人データのサーバー送信ゼロ(Worker は中継のみ・状態保持禁止)
- localStorage / sessionStorage 不使用(状態は IndexedDB かメモリのみ)
- 絵文字ゼロ / アクセント色は `#00C4CC` のみ

## マイルストーン

- **v0.2 → v0.12**: 収集・正規化・重複排除・全文検索・キーワードルール・
  Vault/Markdown/JSON 出力・PWA/オフライン・BYOK 要約・i18n(JA/EN)を実装・出荷。
- **v0.13(現行)**: 単語ウォッチ + ソクラテス式探究(反証条件・Falsifier Watch・
  裁決の変遷・問いの解決)、収集ソース9種、監査ラウンド 6–31(Worker SSRF 対策ほか)。
  自動テスト全通過(件数は `npm test` の出力が正。文書に固定値を書かない)。`main` を出荷済み状態へ同期。
- **v1.1(予定)**: `README.md`「v1.1 予定」を参照(Tauri デスクトップ、端末内推論、
  ベクトル検索の再評価)。いずれも着手前に ADR と North Star チェックを通す。

## リリース完了定義(G10)

`Plan.md` §10 の G10 全7項目(Linter 警告ゼロ / テスト全通過 + モジュール網羅 100% /
脆弱性 Critical・High ゼロ / クロスレビュー / ドキュメント最終確認 / PWA 署名ビルド +
Lighthouse 90+ / ベータ確認)。実測結果は `G10_RELEASE_CHECKLIST.md` に記録する。

> round 44: G10.02 の「カバレジ ≥ 80%」は**測定不能**(本体が index.html のインライン
> モジュールにあり vitest から import できず、実測 0/0)だったため、同じ意図を実測できる
> **モジュール網羅率**へ置き換えた。経緯と限界は `G10_RELEASE_CHECKLIST.md` 参照。
